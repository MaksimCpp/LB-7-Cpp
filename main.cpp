#include <iostream>
#include <vector>
#include <algorithm>
#include "SquareMatrix.h"

template<typename T>
void print(std::vector<T> v) {
    for (int i = 0; i < v.size(); ++i) {
        std::cout << v[i] << " ";
    }
    std::cout << "\n";
}

int main() {
   
}